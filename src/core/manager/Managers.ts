import {EventManager} from "./EventManager";
import {ElementNodeManager} from "./ElementNodeManager";

export interface Managers {
    eventManager: EventManager;
    elementNodeManager: ElementNodeManager
}
